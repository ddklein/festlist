# Frontend Environment Configuration

# Development Server Configuration
# Use localhost for Spotify OAuth compatibility (default)
# Change to 0.0.0.0 for network access (use npm run start:network)
HOST=localhost
PORT=3000

# Backend API URL
# For local development
REACT_APP_BACKEND_URL=http://localhost:8000

# For network access (replace with your server's IP)
# REACT_APP_BACKEND_URL=http://192.168.1.100:8000

# For Docker development
# REACT_APP_BACKEND_URL=http://0.0.0.0:8000

# Development settings
REACT_APP_DEBUG=false

# Build settings
GENERATE_SOURCEMAP=true

# Browser settings (set to 'none' to prevent auto-opening browser)
BROWSER=none

# Fast Refresh (React development feature)
FAST_REFRESH=true
